node_modules/
out/
release/
dist/
*.tsbuildinfo
# Native RDP worker build output is staged here immediately before packaging.
build/rdp-worker/
build/rdp-worker-cmake/
# build/icon.png 由 npm run icon 生成，纳入版本控制以便打包无需额外步骤
# .npmrc 是开发机本地的代理配置，绝不能入库：
# 里面的 proxy 只在本机存在，进了仓库会让 CI 与任何克隆者的 npm 连不上 registry
.npmrc
*.local
*.log
.DS_Store
Thumbs.db

# Android/Gradle build output
android/.gradle/
android/**/build/
# CMake/NDK intermediate output for the privileged PTY host. It sits outside build/, so it needs its
# own entry — it contains generated makefiles and absolute toolchain paths.
android/**/.cxx/
android/local.properties

# Local Android toolchain downloads (never commit SDK/Gradle binaries or caches)
.local/android-sdk/
.local/gradle/
.local/gradle-user-home/
.local/jdk17/
.npm-cache/
.superpowers/
