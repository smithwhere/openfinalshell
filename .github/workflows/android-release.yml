name: android-release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing version tag to build and publish"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: android-release-${{ inputs.release_tag || github.ref_name }}
  cancel-in-progress: true

env:
  NODE_VERSION: "24"
  JAVA_VERSION: "17"
  GRADLE_VERSION: "8.10.2"
  ANDROID_PLATFORM: "android-35"
  ANDROID_BUILD_TOOLS: "35.0.0"
  # Required to configure :app at all — it builds the privileged PTY host through CMake, so a
  # missing NDK fails every Gradle task in this job, not just the native one.
  ANDROID_NDK: "27.0.12077973"
  ANDROID_CMAKE: "3.22.1"
  RELEASE_TAG: ${{ inputs.release_tag || github.ref_name }}

jobs:
  release:
    name: Build and publish Android APK/AAB
    if: github.ref != 'refs/tags/v0.30.29' && inputs.release_tag != 'v0.30.29'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout tagged source
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.release_tag || github.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install Android SDK packages
        shell: bash
        run: |
          # `yes` is killed by SIGPIPE when sdkmanager stops reading, and GitHub Actions bash runs
          # with `set -o pipefail`, so an unguarded pipeline would fail the step. The package
          # install below is what actually proves the licenses were accepted.
          yes | sdkmanager --licenses > /dev/null || true
          sdkmanager "platform-tools" "platforms;${{ env.ANDROID_PLATFORM }}" "build-tools;${{ env.ANDROID_BUILD_TOOLS }}" "ndk;${{ env.ANDROID_NDK }}" "cmake;${{ env.ANDROID_CMAKE }}"
          # :app cannot even configure without these, so a silent no-op install has to fail here
          # rather than surface later as an unrelated-looking Gradle error.
          test -f "$ANDROID_HOME/ndk/${{ env.ANDROID_NDK }}/source.properties" || {
            echo "::error::NDK ${{ env.ANDROID_NDK }} was not installed"; exit 1; }
          test -d "$ANDROID_HOME/cmake/${{ env.ANDROID_CMAKE }}" || {
            echo "::error::CMake ${{ env.ANDROID_CMAKE }} was not installed"; exit 1; }

      - name: Generate shared schema metadata
        run: node scripts/generateSharedSchema.mjs

      - name: Verify generated schema is committed
        run: git diff --exit-code -- src/shared/generated android/core/src/main/kotlin/io/github/openfinalshell/android/core/protocol

      - name: Validate tag and package version
        shell: bash
        run: |
          pkg=$(node -p "require('./package.json').version")
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "::error::Expected an existing release version tag"; exit 1; }
          git show-ref --verify "refs/tags/$RELEASE_TAG" >/dev/null || { echo "::error::Release tag not checked out"; exit 1; }
          tag=${RELEASE_TAG#v}
          echo "package.json=$pkg tag=$tag"
          if [ "$pkg" != "$tag" ]; then
            echo "::error::tag $RELEASE_TAG does not match package.json version $pkg"
            exit 1
          fi

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: ${{ env.GRADLE_VERSION }}

      - name: Check Settings translations and run Kotlin unit tests
        shell: bash
        run: |
          log=$(mktemp)
          if gradle -p android :app:checkI18n testDebugUnitTest --stacktrace > "$log" 2>&1; then
            tail -3 "$log"
            exit 0
          fi
          tail -40 "$log"
          # Publish the failure as check annotations. Annotations are readable without repository
          # write access, while job logs and artifacts are not — so this is the one channel that
          # makes a CI-only failure legible from outside the repo.
          grep -E "> Task .* FAILED|FAILED$|Lint found|error:|What went wrong|Execution failed for task|AssertionError|TimeoutCancellation|expected:|saw:" "$log" \
            | tail -8 | sed 's/%/%25/g' | while IFS= read -r line; do echo "::error::$line"; done
          exit 1

      - name: Validate signing secrets
        env:
          ANDROID_KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_B64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          test -n "$ANDROID_KEYSTORE_B64" || { echo "::error::Missing ANDROID_KEYSTORE_B64"; exit 1; }
          test -n "$ANDROID_KEYSTORE_PASSWORD" || { echo "::error::Missing ANDROID_KEYSTORE_PASSWORD"; exit 1; }
          test -n "$ANDROID_KEY_ALIAS" || { echo "::error::Missing ANDROID_KEY_ALIAS"; exit 1; }
          test -n "$ANDROID_KEY_PASSWORD" || { echo "::error::Missing ANDROID_KEY_PASSWORD"; exit 1; }

      - name: Restore release keystore
        env:
          ANDROID_KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_B64 }}
        run: printf '%s' "$ANDROID_KEYSTORE_B64" | base64 --decode > "$RUNNER_TEMP/openfinalshell-release.jks"

      - name: Assemble signed APKs and AAB
        env:
          ANDROID_KEYSTORE_PATH: ${{ runner.temp }}/openfinalshell-release.jks
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          version=$(node -p "require('./package.json').version")
          gradle -p android :app:assembleRelease :app:bundleRelease \
            -PrequireReleaseSigning=true -PversionName="$version" -PversionCode="$GITHUB_RUN_NUMBER" --stacktrace

      - name: Normalize Android release assets
        shell: bash
        run: |
          version=$(node -p "require('./package.json').version")
          mkdir -p android-release
          shopt -s nullglob
          apks=(android/app/build/outputs/apk/release/*.apk)
          aabs=(android/app/build/outputs/bundle/release/*.aab)
          expected_abis=(arm64-v8a armeabi-v7a x86_64 x86 universal)
          [ "${#apks[@]}" -eq "${#expected_abis[@]}" ] || {
            echo "::error::Expected ${#expected_abis[@]} release APKs, found ${#apks[@]}"
            printf '%s\n' "${apks[@]}"
            exit 1
          }
          [ "${#aabs[@]}" -eq 1 ] || { echo "::error::Expected exactly one release AAB, found ${#aabs[@]}"; exit 1; }
          for abi in "${expected_abis[@]}"; do
            apk="android/app/build/outputs/apk/release/app-$abi-release.apk"
            [ -f "$apk" ] || { echo "::error::Missing release APK for ABI $abi"; exit 1; }
            cp "$apk" "android-release/OpenFinalShell-$version-android-$abi.apk"
          done
          cp "${aabs[0]}" "android-release/OpenFinalShell-$version-android.aab"

          # A release artifact must be installable and signed. apksigner is part of
          # the pinned Android build-tools package installed above.
          apksigner="$ANDROID_HOME/build-tools/${ANDROID_BUILD_TOOLS}/apksigner"
          [ -x "$apksigner" ] || { echo "::error::Missing apksigner at $apksigner"; exit 1; }
          for apk in android-release/*.apk; do
            "$apksigner" verify --min-sdk-version 26 "$apk"
          done

          # Keep the release directory deterministic: five APKs plus one AAB and
          # the Android-only checksum file, with no Gradle metadata accidentally uploaded.
          [ "$(find android-release -maxdepth 1 -type f -name '*.apk' | wc -l)" -eq 5 ] || exit 1
          sha256sum android-release/*.apk android-release/*.aab | tee android-release/SHA256SUMS-android.txt
          ls -lh android-release

      - name: Upload Android build artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release-${{ env.RELEASE_TAG }}
          path: android-release/*
          if-no-files-found: error
          retention-days: 30

      - name: Ensure GitHub Release exists
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          # Android and desktop release jobs start together. Either job may create
          # the Release first; retry briefly to absorb a concurrent create or API
          # propagation delay, without waiting for the desktop build itself.
          for attempt in $(seq 1 12); do
            if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
              echo "Release $RELEASE_TAG is available"
              exit 0
            fi
            gh release create "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --title "OpenFinalShell $RELEASE_TAG" \
              --notes "Android assets are being uploaded; desktop assets will follow." \
              --verify-tag >/dev/null 2>&1 && exit 0 || true
            echo "Waiting for Release $RELEASE_TAG ($attempt/12)"
            sleep 5
          done
          echo "::error::Unable to create or locate Release $RELEASE_TAG"
          exit 1

      - name: Remove stale Android assets
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --json assets --jq '.assets[].name' |
            while IFS= read -r asset; do
              case "$asset" in
                *-android-*.apk|*-android.aab)
                  gh release delete-asset "$RELEASE_TAG" "$asset" --repo "$GITHUB_REPOSITORY" --yes
                  ;;
              esac
            done

      - name: Upload Android assets to Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "$RELEASE_TAG" android-release/* --repo "$GITHUB_REPOSITORY" --clobber
